# Lab Challenger A — Tighter Breaker (REPLAY-READY)
# Champion'dan tek-key fark: monthly_loss_pct_long 0.15 -> 0.12
# Bu dosya Lab tournament için scaffold — production'a kopyalanmaz.
#
# Inherit: configs/risk_balanced.yaml
# Override: aşağıdaki anahtarlar
#
# Expected: ROI +%212-227 / DD -%30.7-32.7 (champion -4..-7pp ROI, -3..-5pp DD)
# Bonferroni p_threshold: 0.05/3 = 0.0167

challenger_metadata:
  id: A
  name: "tighter_breaker_long_12pct"
  champion_baseline: "v2.0.3"
  bonferroni_p_threshold: 0.0167
  rollback_ready: true
  status: REPLAY_READY
  code_changes_required: false
  expected_annual_return_min: 2.12
  expected_annual_return_max: 2.27
  expected_max_dd_min: 0.307
  expected_max_dd_max: 0.327

drawdown_breakers:
  # Side-conditional asimetri tighten — long limitini %15'ten %12'ye çek
  monthly_loss_pct_long: 0.12
  monthly_loss_pct_short: 0.05  # aynı (SEC26.B-1 wiring)
  # Diğer breaker'lar champion'dan inherit
