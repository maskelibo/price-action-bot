# RESUME — 2026-06-15 (08:57 TR itibarıyla)

Önceki handoff: `RESUME_2026-06-10.md`. Ölçüm metodolojisi + compounding dersi orada (hâlâ geçerli). **Bu oturum:** split-brain çözümü + branch temizliği + main merge + CT-EXE-02 teşhis/fix/deploy + dürüst PnL/pyramid analizi.

## ⚡ İLK YAPILACAKLAR (yeni session)

1. `pgrep -fl "futures_daemon.*15m"` → **TEK PID** (v14, 94921 veya KeepAlive respawn'ı) görünmeli. İki tane görünürse split-brain GERİ DÖNMÜŞ — bkz aşağıda.
2. "durum nedir / pnl" gelirse: **sadece v14**, borsa-truth (`fapiPrivateGetIncome`), unrealized=LAST, TR saati. Script hazır (aşağıda).
3. **İzlenecek tek kritik sinyal:** bir pozisyon ilk kez **+1.2R**'a ulaştığında `grep PYRAMID logs/futures_daemon_v14.log` — leg-fill ateşliyor mu? (parite testi, bkz aşağıda).

## ⚠️ BU VERSİYON NE ÇALIŞTIRIR — KRİTİK

- Canlı bot = **v14** (`scripts/futures_daemon_v14.py --timeframe 15m`), config `risk_phoenix_scalp_15m_v14p3.yaml`, journal `data/futures_journal_v14.duckdb`, **launchd `com.priceaction.futures_v14`** (KeepAlive). = champion + WIDESTOP + pyramid + F2/F4 + Grimes katmanları.
- **Honest backtest beklentisi: +17-18.5%/ay (Grimes'la +26%), DD −19..−24.** Bu **fixed-base (taze-$10k/ay, compounding'SİZ, 55-57bps honest fee)** hesaplandı — compounding-şişiği DEĞİL (README satır 37/40/112/144 doğruluyor). AMA **kanıtlanmamış BACKTEST**.
- **PYRAMID CANLIDA HİÇ ATEŞLENMEDİ (0/21 registered).** +18.5% vs pyramid'siz ~%7 base farkı (~11 puan) TAMAMEN pyramid'den gelir → şu ana kadar canlıda **sıfır** katkı. Neden: hiçbir pozisyon +1.2R'a ulaşmadı (en iyi DOT +0.57R). Mekanizma bozuk DEĞİL, ama parite **test edilMEDİ**. İlk +1.2R kazanan = parite testi.
- **Net honest:** canlı performans şu ana kadar = pyramid'siz base senaryo, o da küçük/kontamine sample'da ekside. "+%20 geliyor" KANITLANMADI.

## Bot Durumu (devir anı, 08:45 TR)

- **v14 ÇALIŞIYOR**: PID 94921, uptime ~6.8s (15 Haz 02:12 TR restart'tan beri — CT-EXE-02 deploy). Son tick 08:45 TR sağlıklı, breaker temiz, WIDESTOP+pyramid aktif.
- **TEK 15m daemon** (champion emekli edildi, bkz aşağıda). futures5m ayrı launchd (dokunma).
- **Açık pozisyonlar (5):** DOGE short 4183@0.0886 · XRP long 310.5@1.171 · DOT long 754.1@0.975 · UNI short 147@2.52 · AVAX long 108@6.711. Unrealized **+$11.92** (DOT +12.82 önde).

## Bu Oturumda Yapılanlar

**1. Split-brain çözüldü (kritik ops bulgu):** ~2 gündür (13 Haz reboot'tan beri) emekli champion 15m (`futures_daemon.py`, journal `futures_journal.duckdb`) + v14 **aynı testnet hesabını AYRI journal'larla** yönetiyordu. NEAR: champion 300 + v14 350 = borsa 650 netledi → yanlış-boyutlu SL/TP. Champion **kalıcı emekli**: bootout + disable + plist→`.retired-20260615`. Tek 15m daemon doğrulandı. (memory `champion-15m-launchd-keepalive` güncel — bu 2. tekrar; bootout TEK BAŞINA yetmez, plist'i de taşı.)

**2. Branch temizlendi + main merge.** Commit edilmemiş 26 dosya (bitmiş iş) 4 grupta, 424 untracked provenance/script/scratch ayrıştırıldı:
- `c62c584` backtest fiyat-yüzde trail parity + trail-mult 1.5 revert + researcher memory
- `1e6995a` DuckDB zehirli-handle reconnect + journal partial-closes schema v3
- `9af8ae9` audit 1-gün SLA/remediation/REOPENED/escalation + bot_monitor live-equity
- `56a71dd` + `1de850e` runtime artefakt + gitignore temizliği
- `ae5352e` 267 araştırma provenance (hypotheses+results) + ajan kararları + 2 RESUME
- `44ded01` session_orb stratejisi + 3 yeni test + config/launchd
- `b5e7091` 59 research/deploy/analiz scripti
- **main `d7b7ed4`→`1de850e`'e ff'lendi** (working-tree/daemon etkilenmedi).

**3. CT-EXE-02 (AF-EXE-20260531-001) teşhis + fix + deploy:**
- `f8bd7ec` birleşik income-tabanlı PnL yazıcı. Kök neden: journal realized_pnl lokal `(exit-entry)*qty` (fee/funding atlanıyor) + `JOURNAL_HEAL` yolu HİÇ PnL yazmıyordu (ATOM/ZEC −80 gizleniyordu). reconcile_orphan exit-price makinesi pratikte ölü kod (0 çağrı).
- Çözüm: `exchange_income.fetch_realized_income` (best-effort, hata→None→eski davranış, kapanışı asla bloklamaz) + `record_close(realized_pnl_override)` + `get_partial_pnl_sum` (çift-sayma yok). Heal + in-daemon + reconcile yolları bağlandı. 8 yeni test + 393 regresyon yeşil.
- **DEPLOY EDİLDİ:** v14 `launchctl kickstart -k` ile restart, 5 pozisyon temiz re-adopte, sağlıklı.
- **⚠️ REGRESYON bulundu + düzeltildi (`c393466`):** `f8bd7ec`'in heal-PnL kodu canlıda `JOURNAL_HEAL_PNL_ERR` (DuckDB ro+rw "different configuration") veriyordu → income yazılamıyordu + muhtemelen 09:14 (15 Haz) restart'ına katkı. Fix: heal-PnL yazımı `_heal_con` kapandıktan sonraya ERTELENDİ + in-daemon partial-sum açık con'da INLINE + eşzamanlılık regresyon testi. Yeniden deploy edildi (PID 20648). **Canlı heal-PnL teyidi ilk heal olayında** (`grep JOURNAL_HEAL_PNL logs/futures_daemon_v14.log` — `_ERR` değil success bekle). DERS: canlı daemon fix'i eşzamanlılık senaryosunu da test et, izole birim testi yetmez.

### Yapılmayan / yarım kalan
- **main 1 commit geride** (`f8bd7ec`) — `git branch -f main HEAD` ile ff edilebilir (onay bekliyor, push edilmedi — repo yerel).
- **CT-EXE-02 findings register'da hâlâ OPEN** — kasıtlı (audit bağımsızlık): denetçi (audit_execution) deploy sonrası ilk kapanışta `JOURNAL_HEAL_PNL` doğrulayıp kontrolü yeniden PASS alana dek kapanmaz.
- **Geçmiş şişik journal kayıtları geriye düzeltilmedi** (ayrı backfill işi).
- Otonom researcher ajanı yeni backtest_results üretmeye devam ediyor (15 Haz tarihli untracked dosyalar) — commit edilmedi.

## HEDEF: dürüst aylık ≥%10 — DURUM

- **v14 backtest:** +17-18.5%/ay (Grimes +26), DD −19..−24, fixed-base honest. **Tüm gate'ler geçti** (OOS, sembol-çıkarma, sign-flip null p<0.001). AMA **canlı kanıt YOK**.
- **Canlı (4.5 gün, borsa-truth):** NET realized **−$151.46** (24 kapanış), unrealized +$11.92, toplam **−$139.54**, cüzdan $4846.70 (başlangıç ~$5000).
  - Kaybın **%57'si (−$77) split-brain BUG** (bugün kapandı), gerisi şişik-baseline'a karşı küçük-sample gürültüsü.
  - **Pyramid 0/21 ateşledi** → +18.5%'in motoru hiç çalışmadı; canlı = pyramid'siz ~%7 base senaryosu (o da küçük sample'da ekside).
  - Champion-sonrası temiz dönem: **0 kapanış** — temiz ölçüm DAHA YENİ başlıyor.

## AÇIK KARARLAR

1. **main ff** → `f8bd7ec` main'e alınsın mı? (önerim: evet, canlı kodu yansıtır.)
2. **Pyramid parite testi** — ilk +1.2R kazananda leg-fill ateşliyor mu izlenmeli; ateşlemezse "parite kırığı" kanıtı → gerçek beklenti ~%7/ay'a düşer.
3. **CT-EXE-02 kapanış** — ilk kapanış olayından sonra `JOURNAL_HEAL_PNL`/`PROT_INCOME` doğrula + audit_execution ile re-verify → register'da CLOSE.
4. **Temiz forward-test ≥2-3 hafta** — iki kontaminasyon kalktı; v14 edge'ine ancak bundan sonraki temiz veriyle karar verilebilir.

## Dürüst Şerhler

- **Backtest = hipotez, fill = kanıt.** v14'ün +17-18.5%/ay'ı DÜRÜST (compounding'siz) ama **canlıda doğrulanmadı**; en kritik motor (pyramid) henüz ateşlenme şansı bile bulmadı.
- **Pyramid leg-fill oranı canlı doğrulanmadı** — backtest haircut'la modelledi; +18.5'in tüm farkı buna bağlı. Canlıda modellendiği gibi dolmazsa beklenti ~%7/ay.
- **−$151 "edge öldü" kanıtı DEĞİL** (24 trade, −19..−24 DD bandına karşı hiç; çoğu bug) ama "%20 geliyor" da DEĞİL. İkisi de henüz veriyle desteklenmiyor.
- **Journal'a hâlâ tam güvenme** — CT-EXE-02 fix canlı ama ilk kapanışta doğrulanana dek (+ geçmiş kayıtlar şişik). Ölçüm = borsa income.
- Split-brain 2 kez yaşandı; yeni 15m bot deploy ederken eski plist'i MUTLAKA taşı/sil (bootout yetmez).

## PnL ölçüm script'i (borsa-truth, hazır)

```
.env yükle → ccxt binance testnet (BINANCE_FUTURES_TESTNET_API_KEY/SECRET, manuel fapi URL override)
fapiPrivateGetIncome (REALIZED_PNL + COMMISSION + FUNDING_FEE) since 2026-06-10 22:04:44 UTC
+ fetch_positions unrealized = (last-entry)*qty (LAST, mark değil). Rapor TR.
```
(Bu oturumda kullanılan tam script transcript'te; tek-daemon olduğu için artık kontaminasyonsuz.)
