# RESUME — 2026-05-28 (Bot canlı: silent fail çözüldü, 2 trading bot paper deploy)

## ⚡ KISA ÖZET

Bu seansta **3 audit turu** yapıldı (regular A1-Y10 + hard review F1-F11 +
re-audit yan etki). En kritik bulgu: **PID 20788 futures15m bot 4+ saat
"canlı görünüp aslında hiç sinyal taramamış"** — CEO daemon market.duckdb'yi
default-write mode'da açıyordu → exclusive lock → futures15m read fail
silent → 0 sinyal, 0.0s latency, app.log'da gizli, futures_daemon.log'da
"normal" görünüyor.

**Şu an: 3 trading servis canlı paper modda** (ceo + futures15m + futures5m).
Sonraki bar 19:15 UTC. Pytest 2035 PASS / 0 FAIL. Bot kanıt-bazlı çalışıyor.

⚠️ **Tüm değişiklikler UNCOMMITTED** (~50 dosya local diff). Commit kararı
ayrı: bu seans sonunda kullanıcı kararıyla yapılacak.

## ⚠️ BU VERSİYON NE ÇALIŞTIRIR — KRİTİK

**3 launchd-managed servis + 2 cron job, hepsi paper mode (testnet).**

### 1. com.priceaction.futures15m — PID 31926 (ana trading bot)
- Strategy: **Phoenix wide-stop v2 (VSA amplify)** — vsa_climax_test vol_z=2.0
- Config: `risk_phoenix_scalp_15m_widestop_vsa2.yaml` (SHA 53a9a4768ee4a4a6)
- Filtre: sl_pct_min=0.025 (WIDESTOP AKTİF), leverage_max=3x, pyramid KAPALI
- Backtest 5y: aylık ortalama **+%14.08**, DD -%17.68, 0/61 negatif ay,
  OOS aylık +%19.30, annual +%485.6.
- Honest: BACKTEST = hipotez. Canlı fill ekonomisi (post-only oran, gerçek
  slippage) HENÜZ doğrulanmadı.

### 2. com.priceaction.futures5m — PID 35137 (YENİ deploy)
- Strategy: **P1c (5m candidate)** — W1 base + M2d DD + M3a vol_z + 14d halt
- Config: `risk_phoenix_scalp_5m_p1c.yaml` (SHA 161ecc1110e95183)
- Filtre: sl_pct_min=0.030, daily breaker %2.00
- Backtest 5y: aylık ortalama **+%15.25**, DD -%16.5, worst month -%3.61
  (structural sınır), n/yr=537, avg_risk_pct ~0.00485
- Walker history (önceki test'lerden): equity=$1900, 18 trade, MTD +%90
- Honest: 15m'den AYRI journal (`futures_journal_5m.duckdb`), ayrı PnL.

### 3. com.priceaction.ceo — PID 31720 (orchestration)
- LLM agent loop, daily/weekly brief Telegram'a push
- **FIX 2026-05-28 (audit-FIX-VER3-1):** `PA_DUCKDB_READ_ONLY=true` plist'e
  eklendi (bu olmadan tüm sistem 4+ saat silent fail'di).

### 4. com.priceaction.dbbackup — calendar cron (00:00 UTC)
- `scripts/backup_duckdb.sh` — 14 .duckdb dosyası → `data/backups/YYYYMMDD/`
- 7-day rolling retention (~6 GB).

### 5. com.priceaction.logrotate — calendar cron (04:00 UTC)
- `scripts/rotate_logs.sh` — launchd log'lar 50MB üstünde gzip + truncate.
- 30 günlük arşiv retention.

## Bot Durumu (devir anı — 22:05 TR / 19:05 UTC)

```
launchctl list | grep priceaction:
  -      0    com.priceaction.dbbackup     (cron, bekliyor)
  -      0    com.priceaction.logrotate    (cron, bekliyor)
  31926  -9   com.priceaction.futures15m   (canlı, uptime ~3.5 saat)
  31720  0    com.priceaction.ceo          (canlı)
  35137  0    com.priceaction.futures5m    (canlı, uptime ~13 dk)
```

**Açık pozisyon: 0** (her iki bot için).

**Son scan'ler (gerçek scan — fix sonrası):**
- 15m son: `[19:00:24Z] 15M_TICK_DONE: scan=6.6s pos_monitor=6.3s`
- 5m son: `[19:05:10Z] 5M_TICK_DONE: scan=5.4s widestop=0 accept=0`

Latency'ler **normal pattern**'de (5-8s) — 0.0s silent-fail bug ÇÖZÜLDÜ.

**Pytest:** 2035 PASS / **0 FAIL** / 131 skip. 13 pre-existing fail
F7'de temizlendi.

## Bu Oturumda Yapılanlar

### Audit turları (3 kez)

1. **Regular audit (A1-A12 + Y1-Y10 + D1-D3)** — daha önceki bir seans tüm
   bunları yaptı (memory'de yazılı).
2. **Hard review (Eksen 1-9)** — 9 paralel Explore ajan, ~48 yeni bulgu.
   En kritik 0.0s scan latency yakalandı.
3. **Re-audit yan etki (F10)** — F1-F9 fix'lerinin yarattığı yeni sorunlar.
   10 bulgu, çoğu kabul edilebilir veya STALE; 1 gerçek (CEO template plist)
   düzeltildi.

### Bu oturumda fix gruplarıyla yapılanlar

- **VER-3 (0.0s scan latency root cause):** CEO plist'ine
  `PA_DUCKDB_READ_ONLY=true` ekle (loaded + repo template ikisi de).
  `futures_trade_15m._scan_symbol` read_fail counter + stderr summary.
  Bot manuel scan 0.02s/0 sinyal → 8.49s/1 sinyal'e döndü, sonra live'da
  6.2s/3 sinyal.
- **F1:** `forex_bot/` → `archive/forex_bot_legacy_20260528/`, RUNBOOK.md
  Windows hardcoded path → relative, ghost v11/v63 plist + log archive'a.
- **F2:** `scripts/rotate_logs.sh` + `com.priceaction.logrotate.plist`.
  ceo.stdout.log 9.3MB → 334KB.gz manuel rotation.
- **F3:** `compute_kpis` Sharpe annualization fix — equity daily resample
  + ppy=365 zorla. Önceki 15m × 35040 amplify 3-5× abartılı Sharpe veriyordu.
  Pool revalidation parity hâlâ geçiyor.
- **F4:** `scripts/restore_duckdb.sh` (interactive), `exec_with_checkpoint`
  merkezi wrapper store.py'da. Tüm write path'lere uygulamak BACKLOG'da.
- **F5:** Circuit breaker persistent state JSON. Lazy load + corrupt-tolerant.
- **F7:** **13 pytest fail → 0 fail.** yfinance pytest.importorskip,
  Windows path skipif, timing race 30→29.95, realized_pnl_today fix,
  circuit test izolasyonu.
- **F8:** `RECOVERY.md` — 5 acil senaryo (silent scan, DB corrupt, bot ölü,
  veri delik, throttle loop) komut + beklenen çıktı.
- **F9:** `docs/REFACTOR_BACKLOG.md` — variant DB consolidation, circular
  import, 87 threshold, strategies code-reuse → sonraki major release.
- **F11:** futures5m bootstrap. 3 trading servis canlı.

### YAPILMAYAN / yarım kalan

- **Commit henüz yapılmadı** — `git log --since="12 hours"` boş. ~50 dosya
  modified, archive/'a hareket'ler, yeni dosyalar var.
- **Variant DB consolidation** (11 DB → 1 DB + bot_name kolon) — BACKLOG.
- **Circular import (risk/breaker ↔ sizing)** — BACKLOG.
- **87 hardcoded `0.025` threshold** → `_thresholds` import — BACKLOG.
- **Strategies code-reuse (70 strateji)** — BACKLOG.
- **Pool revalidation v2 (vsa amplify)** — `verify_widestop_parity` sadece v1
  conservative'i test ediyor. v2 için ayrı script gerek (memory'de annual +%485.6
  rakamı daemon banner'dan, ayrı verify edilmedi).
- **Telegram secret → `~/.zshrc` migration** — Y5 docs yazıldı, manuel
  taşıma kullanıcı işi.
- **Live deploy** — paper sonrası karar.

## HEDEF: dürüst aylık ≥%10 — DURUM

| Strateji | Backtest aylık | DD | Negatif ay | Gate | Durum |
|---|---|---|---|---|---|
| **futures15m widestop_vsa2** | +%14.08 | -%17.68 | 0/61 | %10 ✓ | paper running |
| **futures5m P1c** | +%15.25 | -%16.5 | ?/~59 | %10 ✓ | paper running |
| rsi2 (arşivde) | bilinmiyor | bilinmiyor | bilinmiyor | — | arşivlendi |
| vwap (arşivde) | bilinmiyor | bilinmiyor | bilinmiyor | — | arşivlendi |

⚠️ Backtest rakamları HIPOTEZ. **F3 Sharpe fix sonrası lab/iterate
script'leri yeniden hesaplanmadı** — eski "Sharpe > 1.5 geçti" kararları
düşmüş Sharpe rakamlarıyla yeniden değerlendirilmeli. CAGR ve DD doğru
(formül matematiksel tutarlı), sadece Sharpe etkilendi.

## AÇIK KARARLAR

1. **Commit kararı:** ~50 dosya uncommitted. Bu seansın değişiklikleri tek
   commit mi (büyük), faz başına gruplu mu? Kullanıcı kararı.
2. **1 hafta paper izleme:** Bot şimdi rahat bırakılacak. Eşik: 7 gün
   kesintisiz çalıştı + 0 silent fail + sinyaller geliyor → "stabil" denilir.
3. **Live geçiş:** 1 hafta paper sonrası karar. Kullanıcı + Principal sign-off.
4. **Eski rsi2/vwap'ı geri yükleme:** Şu an arşivde. Geri istenirse
   `archive/launchd_legacy_20260528/`'den `~/Library/LaunchAgents/`'a kopyala.
5. **Variant DB consolidation:** Paper bot 1-2 hafta stabilize olunca yapılır
   (bot'ları durdurmadan veri kaybı riski var).

## Dürüst Şerhler

1. **Backtest = hipotez, fill = kanıt.** futures15m +%14.08, futures5m +%15.25
   rakamları 5y backtest'ten. Canlı slippage, post-only fill oranı, gerçek
   API gecikmesi henüz ölçülmedi.

2. **0.0s scan latency bug'ını BEN yarattım, BEN buldum.** A1 fix'inde
   launchctl bootstrap ile CEO + futures15m'i aynı anda yükledim. İlk kez
   ikisi paralel çalıştı, CEO'nun yanlış write mode'u açığa çıktı. Eski
   manuel daemon'da bu sorun yoktu. **3. audit'in kendi yarattığı bug'ı
   bulması — "audit bitmez" gerçeğinin pratik kanıtı.**

3. **Ajan iddiası ≠ kanıt — yine kanıtlandı.** Bu hard review'da 4 ajan
   iddiası yanlış çıktı:
   - "CEO plist'inde PA_DUCKDB_READ_ONLY yok" → installed plist'te VAR
     (sadece ops/launchd/ template'inde yoktu, onu da düzelttim)
   - "rag.store, rag.chunker, data.liquidation_ingest, data.onchain_ingest
     0 import" → gerçekte test'lerde 1-2 import var, F6 iptal edildi
   - "cleanup_pool_backups.py henüz yazılmadı" → Y10'da yazıldı, ajan stale
   - "PID 17267 manuel mode" → eski state, gerçekte PID 31926 launchd-managed

4. **Sharpe annualization (F3) eski script'leri etkiliyor.** Lab, iterate,
   hypothesis_runner Sharpe sayısına göre karar veriyorsa, F3 sonrası
   rakamlar düştü. **"Bu strateji Sharpe 3.5 yapıyor"** sayısı abartılıydı
   muhtemelen 0.6-1.2 gerçek değer. Strateji seçim kararları bu fix sonrası
   yeniden yapılmalı. CAGR ve DD bu fix'ten ETKİLENMEDİ.

5. **Variant DB schema drift var.** `idempotency_vwap.duckdb`'de
   `heartbeat_log` tablo eksik (audit ajanı yakaladı). vwap bot arşivlendi,
   ama eğer geri yüklenirse DMS heartbeat write fail eder. Geri yüklemeden
   önce schema repair gerekecek.

6. **5m walker history'si "MTD +%90"** — bu **gerçek değil**, önceki test
   run'larından kalan state. Yeni canlı PnL'i takip ederken ilk haftada bu
   number'a güvenme — gerçek paper PnL ayrı, MTD sıfırdan başlamış say.

7. **futures5m DMS heartbeat 10s, timeout 600s (10dk).** 15m'inkiyle (20s/30dk)
   farklı. 5m bar hızlı olduğu için daha sıkı. Eğer 5m bot 10dk'da heartbeat
   atmazsa DMS pozisyon flatten eder. Test edilmedi canlıda, ilk hafta dikkat.

8. **Pool v2 (vsa amplify, +%485 annual) parite kanıtlanmadı.**
   `verify_widestop_parity.py` sadece **v1 conservative'i** (+%302 annual)
   test ediyor. Daemon banner'da v2 rakamı (+%485) görünüyor ama bunu
   bağımsız doğrulayan script YOK. v2 deploy'un gerçek rakamı belirsiz.

## Sonraki Seans için "İlk yapılacaklar"

```bash
cd ~/price-action-bot

# 1) Bot durumu — sessiz ölmedi mi?
launchctl list | grep priceaction
# Beklenen: 3 PID + 2 cron entry, hepsi healthy

# 2) Son 24 saat scan özeti
grep "15M_SCAN" logs/futures_daemon.log | tail -10
grep "5M_SCAN" logs/futures_daemon_5m.log | tail -10
# Beklenen: latency 5-8s aralığı (0.0s = yine silent fail)

# 3) Stderr'de read_fail var mı?
tail -10 logs/launchd/futures15m.stderr.log | grep SCAN15M_READ_FAIL
# Beklenen: BOŞ

# 4) Backup atılmış mı?
ls data/backups/$(date +%Y%m%d) 2>/dev/null
# Beklenen: 14 .duckdb dosyası

# 5) Açık pozisyon
grep "POS_CHECK" logs/futures_daemon.log | tail -3
# Beklenen: 0 pozisyon veya gerçek pozisyon

# 6) Commit durumu
git status -s | wc -l
# Beklenen: 0 (eğer dün commit yapıldıysa)
```

---

**RESUME tamamlandı. Bot izleme modunda. Sonraki check-in: 24 saat veya silent
fail/restart loop tetiklendi.**
