# RESUME — 2026-07-11 (pytest güvenlik sınırı + runtime izolasyonu kapandı)

> Devir zamanı: **11 Tem 2026 00:22 TR**. Bu oturumda, 10 Tem kapanış
> sprintinden sonra yarım kalan pytest/canlı-runtime izolasyonu tamamlandı,
> adversarial denetimden geçirildi ve ayrı commit'e alındı. Kullanıcı yeni
> Codex session açacak; makine veya canlı bot restart edilmedi.

## ⚡ YENİ SESSION — İLK YAPILACAKLAR

1. **Önce çalışma ağacını koru:**
   ```bash
   git status --short --branch
   git log -3 --oneline
   ```
   Beklenen HEAD: `0848717 feat(test): isolate pytest from live runtime`.
   Branch `audit-hardreview-20260528`, origin'in **1 commit önünde**; push
   yapılmadı.
2. **Ayrı bırakılan dosyaları otomatik commit'e katma:**
   - `scripts/_pnl_status_now.py` — v15p2 anchor/baseline güncellemesi, modified.
   - `.agents/` ve `.codex/` — untracked kişisel/plugin dosyaları.
3. **Bot snapshot'ını yenile:** `/bot-status` skill'i. Devir anında 2 açık
   pozisyon vardı; pozisyonlar varken daemon restart etme.
4. **Yeni üretim işi seçilecekse** tek hat seç:
   - 1h failed-breakout/trap raf taraması,
   - XS-carry ilk koşusu,
   - maker/slippage gerçek-fill izlemesi.
5. `KALAN_ISLER_2026-07-10.md` en güncel karar defteridir. Eski RESUME'lardaki
   kapanmış CRIT/HIGH listelerini yeniden açma.

## ⚠️ BU VERSİYON NE ÇALIŞTIRIR — KRİTİK

- Canlı çalışan bot **v15p2 testnet**: wrapper `scripts/futures_daemon_v14.py`,
  config `configs/risk_phoenix_scalp_15m_v15p2.yaml`.
- Binance USDM **testnet**, 15m, 18 sembol; aktif stratejiler eşit ağırlıklı
  `vsa_climax_test` + `grimes_abc_pullback`.
- `sl_pct_min=0.025`, işlem başına risk **%1**, pyramid **OFF**.
- Bu oturumdaki commit test/runtime güvenliği içindir. Çalışan PID restart
  edilmedi; kaynak değişiklikleri mevcut proses belleğine alınmadı. Üretim
  varsayılan yolları `PA_RUNTIME_ROOT` unset/boşken geriye uyumlu kalır.
- Pytest artık disposable runtime kullanır. Gerçek `.env` yüklenmez, sırlar
  boşlanır; canlı DB, dış ağ ve işaretsiz subprocess erişimleri fail-closed
  korunur. Bu katman **OS sandbox değil**, defense-in-depth'tir.

## Bot Durumu (devir anı — 00:22 TR)

- **Sistem sağlıklı ✅**
- PID **34731**, uptime **6s 26d**, `STAT=SN` — bar beklemesi normal.
- Son tarama: **00:15:21 TR** (`21:15:21Z`), log mtime **00:15:34 TR**;
  snapshot anında yaklaşık 7 dakika taze.
- Sıradaki planlı bar: **00:30:05 TR** (`21:30:05 UTC`).
- Son scan: 5 sinyalin tamamı WIDESTOP filtresinde reddedildi:
  ATOM, AVAX, FIL, LINK, SOL (`sl_pct < %2.5`).
- **2 açık pozisyon / 6 borsa-tarafı algo koruma emri**:

  | Sembol | Yön | Qty | Giriş | Son | U-PnL snapshot |
  |---|---:|---:|---:|---:|---:|
  | NEAR | SHORT | 394.000 | 1.8710 | 1.8725 | −0.59 USDT |
  | ZEC | LONG | 1.534 | 487.6942 | 504.7000 | +26.09 USDT |

  Toplam log-snapshot U-PnL ≈ **+25.50 USDT**. Bu değer exchange API ile bu
  oturumda yeniden mutabık edilmedi; yalnız `POS_CHECK` kanıtıdır.
- `data/state/positions.json` yok. Açık pozisyon için devir kaynağı daemon logu;
  yeni session'da exchange-truth kontrolü yapılmalı.
- Kümülatif log sayaçları (trade sayısı olarak yorumlama):

  | Metrik | Değer |
  |---|---:|
  | `15M_SCAN` | 622 |
  | `15M_REJECT_WIDESTOP` | 531 |
  | `15M_REJECT_STALE` | 0 |
  | `15M_REJECT_RISK` | 11 |
  | `15M_MISSED_BARS` | 0 |
  | `ACCEPT/ENTRY/FILL/POSITION_OPEN` regex eşleşmesi | 51 |

## Bu Oturumda Yapılanlar

### Commit

- `0848717` — **feat(test): isolate pytest from live runtime**
  - merkezi `RuntimePaths` / `PA_RUNTIME_ROOT` sözleşmesi eklendi;
  - Settings, daemon ailesi, dashboard, paper loop, DMS, heartbeat,
    idempotency, pyramid, slippage ve agent mutable yolları yönlendirildi;
  - test başında `.env` devre dışı + gerçek credential sterilizasyonu;
  - repo runtime yazıları, DuckDB/SQLite bağlantıları, dış ağ ve subprocess
    yetkileri için fail-closed test sınırı;
  - marker'lar: `integration`, `network`, `ops`, `subprocess`; canlı testler
    default kapalı;
  - API/CLI/dashboard/paper testlerinde gerçek runtime yan etkileri kaldırıldı;
  - dry-run LLM audit satırlarının token bütçesini şişirmesi engellendi.

### Doğrulama

- Tam pytest: **2565 passed, 136 skipped, 74.11s**.
- Son hedefli koşu: **112 passed**.
- Pre-commit'in tüm kapıları PASS:
  - AST, TOML/YAML/JSON, private-key, hardcoded-secret,
  - Ruff, Ruff Format, typos, whitespace ve merge-conflict kontrolleri.
- `git diff --cached --check` PASS.
- `mypy` çalıştırılamadı: mevcut venv'de paket kurulu değil. Bu oturumda yeni
  bağımlılık kurulmadı.

### Yapılmayan / yarım bırakılanlar

- Commit **push edilmedi**; branch origin'in 1 commit önünde.
- Canlı daemon restart edilmedi; açık pozisyonlara dokunulmadı.
- `integration`, `live` ve `ops` testleri explicit opt-in olmadığı için
  çalıştırılmadı; 136 skip içinde bu sınıflar ve bilinen scaffold skipleri var.
- `_pnl_status_now.py` bağımsız değişikliği review/commit edilmedi.
- `.agents/` ve `.codex/` bu commit'e alınmadı.
- Marked child process steril env alır fakat child içinde pytest audit hook'u
  yoktur; subprocess marker'ı açık bir güven sınırıdır, OS sandbox değildir.

## HEDEF: dürüst aylık ≥%10 — DURUM

**Önce negatif gerçek:** Canlı fill örneklemi hâlâ aylık ≥%10 hedefini
kanıtlamıyor. Bu oturumda performans re-rate yapılmadı; 51 log regex eşleşmesi
51 kapanmış trade anlamına gelmez.

**v15p2 aktif testnet — backtest referansı:** Config'teki son kayıt iki-kol
eşit portföy için aylık **+%21.36**, continuous DD yaklaşık **−%14.9**,
negatif **1/61 ay**, en kötü ay **−%0.05**, Sharpe **5.33** bildiriyor. Bunlar
in-sample/top-2 konsantrasyon riski taşıyan **BACKTEST rakamlarıdır**; canlı
beklenti veya garanti değildir.

**Replay muhasebe düzeltmesi:** Kapanış defterindeki 374K-trade kontrolü eski
−%17.13 DD yerine dürüst bandı **[−%14.89, −%14.98]** buldu. Bu düzeltme DD
hesabını iyileştirir; edge'i canlı kanıtlamaz.

**1h rafı:** Pool hazır (19 sembol × 5.1 yıl), fakat failed-breakout/trap
taraması bu oturumda koşmadı; **gate sonucu yok**.

**XS-carry:** `funding.duckdb` yaklaşık 115K satır hazır; ilk koşu yapılmadı,
dolayısıyla **edge sayısı yok**.

**Maker/slippage:** Ölçüm kodu düzeltildi fakat yeni gerçek fill'lerle maker
payı/slippage saha doğrulaması bekliyor.

## AÇIK KARARLAR

1. **Git push:** `0848717` yalnız bu Mac'te. Principal onayıyla
   `git push origin audit-hardreview-20260528`.
2. **vol_target (E2):** öneri **EKLE**, fakat canlı davranış değişikliği olduğu
   için Principal onayı olmadan uygulanmamalı.
3. **E13 trail:** ATR-trail / pct-trail kararı en az 40 temiz kapanış verisi
   sonrasında.
4. **healthchecks.io URL:** altyapı kurulu ama URL olmadığı için inert.
5. **İkinci testnet hesabı:** 1h bot için ön şart; DMS hesap-geneli flatten
   yaptığı için aynı hesaba ikinci bot eklenmemeli.
6. **chromadb CRITICAL CVE:** upstream fix yayınlanmadı; pip-audit takibi.
7. **Sıradaki üretim hattı:** 1h taraması / XS-carry / maker-slippage izleme
   arasından biri seçilmeli; üçünü aynı anda açma.

## Dürüst Şerhler

- **Backtest = hipotez, fill = kanıt.** +%21.36/ay ve DD −%14.9 canlı kanıt
  değildir.
- Açık NEAR/ZEC pozisyonları bu oturumda exchange API ile mutabık edilmedi;
  log snapshot'ı taze ama tek kaynak.
- Test izolasyonu kuvvetli bir defense-in-depth katmanıdır; native/child
  süreçler için mutlak OS sandbox garantisi verilmedi.
- Çalışan daemon eski proses belleğinde. Varsayılan prod yolları değişmedi,
  ancak yeni runtime-path kodu ancak sonraki doğal restart'ta yüklenir.
- Gerçek olaylar görülmeden test-yeşil düzeltmeler saha-doğrulanmış sayılmaz:
  DB guard'ları, slippage outlier yolu, DMS log yolu ve subprocess sınırı
  regresyon testli fakat canlı olayla doğrulanmış değildir.
- `_pnl_status_now.py` değişikliğini isolation commit'ine karıştırmamak doğru
  seçimdi; yeni session'da ayrı diff/kanıtla ele alınmalı.

---

**Ana devir kaynakları:** `KALAN_ISLER_2026-07-10.md`, commit `0848717`,
`tests/test_runtime_isolation.py`, `src/price_action/runtime_paths.py`.
