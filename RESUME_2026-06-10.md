# RESUME — 2026-06-10 (18:15 TR itibarıyla)

Önceki handoff: `RESUME_2026-06-03.md` (ölçüm metodolojisi + compounding dersi orada, hâlâ geçerli).

## Canlı durum

- **v13 testnet daemon ÇALIŞIYOR**: PID 6323, `scripts/futures_daemon_v13.py --timeframe 15m`, log `logs/futures_daemon_v13.log`. Son tick sağlıklı (15:15 UTC = 18:15 TR). v13 **unmanaged** (launchd yok) → crash'te elle restart gerekir.
- **Champion 15m EMEKLİ**: `launchctl bootout gui/501 ~/Library/LaunchAgents/com.priceaction.futures15m.plist` ile durduruldu. Tekrar başlatma — kill yetmez, KeepAlive respawn eder.
- **futures5m** ayrı, launchd altında çalışıyor (dokunma).

## v13 performans (son ölçüm, 9 Haz ~01:35 TR)

- Realized NET: **−$356.35** (46 kapanış, borsa income API'den, başlangıç 2026-06-02 23:03 UTC)
- Açık: **XRP short** qty 274.8, entry 1.3664, SL=1.3664 (breakeven), o an unrealized **+$52.65**
- Ölçüm kuralı: SADECE borsa-truth (`fapiPrivateGetIncome`: REALIZED_PNL+COMMISSION+FUNDING_FEE), journal'a güvenme (CT-EXE-02 açık). Unrealized = **LAST price** ile, mark değil. Rapor saatleri TR.

## Bekleyen kararlar (kullanıcıdan cevap YOK)

1. **XRP short**: elle kapatıp ~+$52 realize et (benim önerim) vs breakeven SL ile bekle (round-trip riski). Bot bunu trail EDEMEZ — journal/pyramid kaydı yok, fix öncesi pozisyon. Profit-lock SL koyma denemeleri residual orphan-cancel bug'ı yüzünden iptal ediliyor.
2. **Weekly breaker**: aktif, tüm yeni girişleri blokluyor (`weekly_loss_pct: 0.05` aşıldı, `weekly_halt_days: 2`, reset Pazartesi 00:00 UTC — 8 Haz Pzt geçti, breaker durumunu yeni session'da DOĞRULA: `grep BREAKER_TICK logs/futures_daemon_v13.log | tail -3`).
3. **Opsiyonel fix (onaylanmadı)**: orphan-cancel N-ardışık-tick stateful kontrol (double-stale-read deliği hâlâ açık).
4. **CT-EXE-02** findings register'da bilerek OPEN bırakıldı.

## Bu hafta yapılan fix'ler (branch: audit-hardreview-20260528, hepsi commit'li)

- e7d98bb: HTF 1d filtre onarımı (RO/RW + kolon adı) + watchdog HEAL + -2022 plain-market fallback
- b529a33: monitoring temizliği (health-check glob, reconciler v13 retarget, audit findings kapama)
- f0a727b: entry-verify (girişten hemen sonra borsada SL kontrolü + anında koyma)
- fd80d4c: trailing `_ORIG_INTENDED_SL` stabil cache (yeni pozisyonlar için)
- d513795: orphan-cancel çift-okuma teyidi

## Yeni session'da ilk yapılacaklar

1. `pgrep -fl futures_daemon_v13` — PID 6323 yaşıyor mu?
2. Breaker durumu (yukarıdaki grep) — reset olduysa yeni girişler açılabilir.
3. XRP hâlâ açık mı + güncel PnL → kullanıcıya XRP kararını tekrar sor.
4. "durum nedir" gelirse: sadece v13, borsa-truth, LAST price, TR saati (memory'de kayıtlı).
