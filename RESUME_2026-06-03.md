# RESUME — 2026-06-03 (chat clear öncesi devir notu)

## ŞU ANKİ CANLI DURUM (en önemli)
- **Champion DURDURULDU** (eski PID 53708). **v13 CANLI** — PID 73001, testnet/paper, deploy 2026-06-03 02:03 TR.
- Başlangıç: ~$4943 (≈$5k), 0 açık pozisyon. Cüzdan $10k istenirse testnet faucet MANUEL (`testnet.binancefuture.com` → Get testnet asset).
- 5m daemon (PID 70580) hâlâ ayrı çalışıyor (dokunulmadı).
- **Ölçüm:** `scripts/v13_exchange_truth_tracker.py` — BORSA income'ından (`fapiPrivateGetIncome`+`fetch_positions`), **journal'dan DEĞİL** (journal drift eder: phantom/qty-drift/double-count geçmişi). Baseline kaydı `data/v13_exchange_truth.duckdb`.

## v13 NEDİR
- VSA-WIDESTOP (sl_pct≥0.025) + **HTF-1d EMA50 giriş filtresi** + **BASELINE exit** (trail 1.5, TP1 30%@1R/TP2 30%@1.5R/runner 40%, 30-bar runner time-stop).
- Multi-TF: **5m+15m core** (30m/45m KAPALI — canlı feed doğrulanmadı, B1 blocker).
- Sabit-fraksiyon sizing (compounding YOK), risk %0.5. DB-izole: `PA_BOT_NAME=v13` → tüm journal/log `*_v13`.
- Dosyalar: `configs/risk_v13_testnet.yaml`, `scripts/futures_daemon_v13.py`, swap: `scripts/swap_champion_to_v13.py`, log: `logs/futures_daemon_v13.log`.

## EN KRİTİK DERS (tekrar etme!)
- **Tüm backtest %/ay sayıları GÜVENİLMEZ ve çelişkiliydi:** champion için +%1-2 / "rastgele/edge yok" (V10, p=0.50) / +%38 (v13) / +%75 (reconcile JSON) çıktı. **Sebep: `production_replay` COMPOUNDING sizing → geometrik-ortalama şişmesi ~10-25×.** Sabit-fraksiyon'da gerçek champion edge ~%1-2/ay.
- **+%7.58, +%13, +%15, +%56, +%38 — HEPSİ ŞİŞİKTİ.** Kullanıcıya yanlış %13 verildi, geri alındı. Bir daha backtest %/ay'ına dayanarak vaat VERME.
- **Edge right-tail:** median işlem KAYBEDİYOR (−1.03R), kazanç birkaç dev runner'dan → canlıda gerçekleşmesi şüpheli.
- **`wlr.shuffle_p` KIRIK** (bootstrap-around-mean → her şeye ~0.5). Doğru test = sign-flip null. Lab gate'inde shuffle_p'ye GÜVENME.
- **Tek güvenilir kanıt = CANLI forward + BORSA income.** Backtest değil.

## GOAL DURUMU
- Kullanıcının `/goal`'u: "her ay tutarlı +%15-20, ≤%20 DD". **Ulaşılamaz/doğrulanamaz** (gerçek edge ~%1-2/ay; kaldıraç DD'yi eritir; çeşitlendirici yok — SMC/momentum/funding/continuation hepsi RED, forex SPK-bloklu+kullanıcı istemiyor).
- Kullanıcı goal'u kapatıyor + chat clear atıyor. Goal-task #19 kapatıldı. `/goal` hook'unu KULLANICI temizlemeli.

## AÇIK İŞLER / İZLE
1. **v13 forward:** ilk işlemler/çıkışlar temiz tetikleniyor mu (testnet reduceOnly -2022 quirk'i vardı — DOT/AVAX kapanışında çıktı, plain-market'la çözüldü; v13'ün TP/SL çıkışlarını ilk işlemde kontrol et).
2. Champion sıcak (kodu duruyor) → rollback gerekirse.
3. Haftalarca canlı veri → gerçek %/ay (borsa-truth tracker'dan).

## TÜM RAPORLAR
`reports/research/smc/`: champion_characterization (§11 55bps), RECONCILE_champion_truth, v9-v13 (multi-TF/fee/regime/ensemble/entry/funding/feasibility), multitf_tournament_verdict, GOAL_15PCT_DECISION_BRIEF, MORNING_REPORT (SMC 161-video çalışması — hepsi RED).

## AYRI KONU (DuckDB)
Saatlik `ingest_ccxt.run_hourly` "Invalid bitmask/invalidated" hatası vardı → `store.py` self-healing fix + ceo_loop restart edildi (PID 8856). Audit CT-DAT-04 invalidation sınıfını yakalayacak şekilde genişletildi. SLA 1-gün lifecycle eklendi (audit_base.py).
