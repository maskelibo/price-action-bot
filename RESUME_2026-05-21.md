# RESUME — 2026-05-21 (PC restart öncesi handoff)

## ⚡ YENİ SESSION — İLK BUNLARI YAP

1. **Bot durumunu çek:**
   ```
   python scripts/_status_quick.py      # PnL + pozisyonlar
   python scripts/_orders_check.py      # TP/SL koruma emirleri
   ```
2. **Daemon kapalıysa yeniden başlat** (PC restart'ta öldü):
   ```
   python -u scripts/futures_daemon.py --timeframe 15m
   ```
   Başlangıç log'unda doğrula: `post_only=True`, `flatten AKTİF`,
   `PYRAMID_STORE → Price Action\data`.
3. **Post-only doğrulama koşusunu izle** — ASIL İŞ bu. Bot artık post-only
   maker ile çalışıyor. Birkaç gün gerçek veri biriktikçe: gerçek fill
   oranı + gerçek maliyet ölç → B'nin simülasyonunu (%87.5 fill, pool-R
   +110-164K) doğrula/çürüt. Fill telemetrisi: `data/execution_fills.duckdb`.

## Bot Durumu (PC restart öncesi)
- Daemon **PID 25248**, post-only modu — PC restart'ta ölecek.
- **0 açık pozisyon** (XRP 18:15 UTC'de SL'e takıldı, kapandı).
- NET **−$68 testnet** (−%1.3). USDT $4987, hepsi free.
- Breaker: deadlock DÜZELTİLDİ (aşağıya bak), artık halt yok.

## Bu Session — Hard Review + 18 Bug Fix + Post-Only + Deadlock Fix

**Hard review:** 24-gap audit (10 paralel ajan). Tetikleyici: bot 8 ardışık
SHORT kaybetti → breaker halt. Kök sebep: backtest idealize, canlı parity kırık.

**Commitler (branch `feat/pyramid-be-protect`):**
- `6d4d719` D BE-protect + 15m daemon WIP checkpoint
- `180bbcb` PROT_WATCHDOG SL qty fix (B-2)
- `5ac7cff` Aşama 1: G3 cooldown bypass, G4 yanlış YAML, G10 timeframe, G24 ROOT path
- `44570b1` Aşama 2: G21 DMS flatten, G7 journal +3h tz bug
- `69da4e0` fix batch: G5/G8/G9/G11/G12/G13/G14/G16/G19/G20/G22
- `a5defea` post-only maker execution (G23 + entry wiring)
- `689101d` breaker consecutive-loss DEADLOCK fix (consec_consumed watermark)

**G24 DB taşıma:** `pyramid_store.duckdb` → `Price Action\data\` (kopyalandı).

**Breaker deadlock:** 9 ardışık kayıp → breaker halt → cool-down dolunca
yeniden tetikliyordu (counter journal'dan türetiliyor, halt'ta kazanç
imkânsız) → kalıcı kilit. Fix: `BreakerState.consec_consumed` watermark
(lab.py parity). State dosyası tek seferlik resetlendi (`consec_consumed:9`,
`triggered_consecutive:false`). 137 risk testi PASS.

## DÜRÜST GERÇEK — değişmedi
15m phoenix C2+V5 dürüst beklenti **~%0** (pool-R negatif, taker execution).
Hafızadaki **+%1253/+%1935 GEÇERSİZ** — sıfır-fee/5bps idealize. Post-only
maker kurtarabilir (B simülasyon: pool-R +110-164K) ama **DOĞRULANMADI** —
doğrulama = post-only canlı fill verisi (şu an akıyor).
Raporlar: `reports/lab_scientist/2026-05-21_honest_cost_rebaseline.md`,
`reports/lab_scientist/2026-05-21_filtered_honest_engine_rerun.md`,
`reports/execution_chief/2026-05-21_post_only_viability.md`.

## Kalan İş — post-only doğrulamasından SONRA
Post-only "15m yaşıyor" derse yapılır; "ölü" derse 15m emekli edilir:
- **G1** backtest exit motoru (ATR-trail, runner force-exit) → canlıya port
- **G2** pyramid backtest closed-form modeli → canlı leg execution parity
- **G6/G17** regime + correlation filtreleri lab.py production_replay'e port
- **G18/G15** cron + DuckDB-lock mimarisi (read-replica) — ingest cron canlı
  daemon ile aynı anda market.duckdb açamıyor
Bunlar "bug" değil, günlerce süren mimari yeniden-inşa.

## Karar Çerçevesi
- Post-only fill oranı yüksek + maliyet düşük → 15m kurtuldu → G1/G2 sıraya.
- Fill oranı düşük / maliyet kötü → 15m taker-only fee-mezar → emekli, başka TF.

Detay hafıza: `memory/project_hard_review_2026-05-21.md`,
`memory/feedback_idealized_backtest_numbers.md`.
