# RESUME — 2026-05-22 (Windows → Mac mini devir)

## ⚡ MAC MINI'DE İLK YAPILACAKLAR

1. **Repo'yu al:**
   ```
   git clone https://github.com/maskelibo/price-action-bot.git
   cd price-action-bot
   git checkout feat/pyramid-be-protect
   ```
   (zaten klonluysa: `git pull` + `git checkout feat/pyramid-be-protect`)
2. **Python env + bağımlılıklar** kurulu olsun (`pip install -e .` veya mevcut venv).
3. **Binance testnet API anahtarları** env/config'de (git'te DEĞİL — sende).
4. **Botu başlat:**
   ```
   python -u scripts/futures_daemon.py --timeframe 15m
   ```
   Başlangıç logunda doğrula: `post_only=True`, `flatten AKTİF`,
   `PYRAMID_STORE → <proje>/data`.

## ⚠️ BU VERSİYON NE ÇALIŞTIRIR — KRİTİK

Bu versiyondaki bot **eski C2+V5 stratejisini** çalıştırır + bu oturumun bug
fix'leri. **Honest beklenti hâlâ ~%0** (15m C2+V5 fee-mezar).

Bulduğumuz **+%12.67/ay'lık wide-stop config CANLI BOTTA DEĞİL** — sadece
araştırma scripti olarak commit'li. Bot onu çalıştırmıyor. Onu devreye almak
ayrı bir "deploy" adımı (aşağıda).

## Bot Durumu (devir anı)
- Windows daemon DURDURULDU — Mac mini tek runner olacak (çift bot = çift emir).
- Testnet hesabında açık pozisyon olabilir (LINK + DOT short, exchange-side
  TP/SL korumalı). Mac daemon başlayınca API'den görür; pyramid kaydı taşınmaz
  (`pyramid_store.duckdb` yerel) — pozisyonlar TP/SL ile korumalı, sorun değil.
- `market.duckdb` (705MB) git'te DEĞİL — canlı daemon ona ihtiyaç duymaz
  (borsadan canlı bar çeker). Sadece backtest için gerekir.

## Bu Oturumda Yapılanlar (commit'ler — hepsi push'lu)
- `05b6fea` G24 ROOT path eksik 3. örnek (slippage_tracker + heartbeat_watchdog + engulfing_mtf)
- `719125f` flaky test fix (regime_cache_staleness)
- `ccca02c` pyramid post-only telemetri + 50bps slippage limit
- `a7ab8a1` 1d config fee=0 bug (`risk_phoenix_v204.yaml` execution bloğu)
- `786169e` araştırma artefaktları (scriptler + hipotezler)

## HEDEF: dürüst aylık ≥%10 (Principal /goal) — DURUM

**1d Phoenix:** honest re-baseline +%276/yıl / **geometrik aylık +%11.67** /
gate 4/4 PASS. Portföy-ortalaması hedefine ulaşan kanıtlı hat.

**15m wide-stop edge — BREAKTHROUGH:** 15m havuzunun `sl_pct ≥ %2.5` alt-kümesi
dürüst maliyet altında pozitif. DD-optimize deployable config:
- **sl_pct ≥ %2.5, risk %0.5, daily_dd 0.02, weekly_dd 0.05**
- Honest (taker +55bps, konservatif): continuous-eğri DD **−%20.7**,
  aylık-mean **+%12.67**, neg 3/61, WF OOS **0/34 negatif**
- Post-only +15bps senaryosu: +%23.64/ay
- Yıllık: 2021 +13.6 / 2022 +12.3 / 2023 **+7.6 (zayıf)** / 2024 +13.1 /
  2025 +20.4 / 2026-kısmi **+4.6 (zayıf)** %/ay → 6-yıl ort. ≥%10 ama rejim-bağımlı

## AÇIK KARAR — wide-stop deploy
Wide-stop config'i canlı bota almak için (Principal kararı bekliyor):
1. Engine'e causal `sl_pct_min_filter` config alanı eklemek (entry'de ATR'den
   `sl_pct ≥ eşik` kontrolü — look-ahead yok).
2. 15m config'inde risk %2 → %0.5, daily_dd → 0.02, weekly_dd → 0.05.
3. Daemon restart → bot wide-stop çalıştırır → canlı doğrulama başlar.
Karar verilene kadar bot eski C2+V5'te kalır.

## Dürüst Şerhler
- Tüm +%12.67 / +%276 sayıları **BACKTEST = hipotez**. Kanıt = canlı fill.
- Post-only fill oranı canlı doğrulanmadı (telemetri: `data/execution_fills.duckdb`).
- 2026 YTD zayıf (+%4.6/ay) — uyarı işareti veya küçük-örnek gürültüsü.
- Detay: `reports/lab_scientist/2026-05-2*`, `reports/researcher/2026-05-21_15m_honest_edge_hunt.md`
  (reports/ git-ignored — yerel; gerekiyorsa ayrıca taşı).
